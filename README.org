#+TITLE: Video Pose
#+SUBTITLE: 多视角人员动作规范检测平台

* 当前版本

Video Pose 0.6 已具备单工位四路视频 PoC 工具链:

#+BEGIN_SRC text
Doctor
  |
Calibration
  |
4路录像
  |
YOLO + Tracking + RTMPose
  |
World Projection + Multi-view Identity
  |
Human-Object / Zone Relation
  |
ActionEvent + Rule Engine
  |
Violation / Score
  |
Evidence + Debug Overlay
  |
Golden Replay Evaluation
#+END_SRC

* 环境检查

#+BEGIN_SRC sh
video-pose-doctor --config configs/offline-demo.yaml
#+END_SRC

* 平面标定

#+BEGIN_SRC sh
video-pose-calibrate \
  --input fixtures/calibration/front-correspondences.yaml \
  --output front-calibration.yaml
#+END_SRC

* 四路离线分析

#+BEGIN_SRC sh
video-pose-analyze \
  --config configs/offline-demo.yaml \
  --output result.json \
  --evidence-dir output/evidence \
  --debug-overlay-dir output/debug
#+END_SRC

Debug 输出:

#+BEGIN_SRC text
front-debug.mp4
rear-debug.mp4
left-debug.mp4
right-debug.mp4
#+END_SRC

* Golden Replay

#+BEGIN_SRC sh
video-pose-evaluate \
  --dataset datasets/golden-v1/manifest.yaml \
  --min-action-f1 0.90 \
  --min-violation-f1 0.90 \
  --min-session-accuracy 0.95
#+END_SRC

* 安装

#+BEGIN_SRC sh
pip install -e '.[dev,video,yolo]'
#+END_SRC

MMPose/MMCV/PyTorch/CUDA 必须按目标 GPU 环境固定版本.

* 文档

详细设计见 =docs/=.
