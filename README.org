#+TITLE: Video Pose
#+SUBTITLE: 多视角人员动作规范检测平台

* 当前阶段

当前代码已经形成可替换模型的视频分析骨架:

#+BEGIN_SRC text
4-View Video Timeline
   |
ReplayPlanner
   |
Synchronized FrameSet
   |
FrameLoader
   |
CameraPerceptionModel
   |
Observation
   |
Semantic Fusion
   |
Explainable Action Baseline
   |
ActionEvent
   |
Rule Engine
   |
Step State + Violation
#+END_SRC

=FrameLoader= 与 =CameraPerceptionModel= 分离. 因此同一个模型后续可以复用在
离线 MP4, 本地录像和实时视频源上.

* 快速开始

#+BEGIN_SRC sh
python -m venv .venv
source .venv/bin/activate
pip install -e '.[dev]'
pytest
video-pose-replay \
  --events fixtures/replay/wrong-tool.jsonl \
  --rules fixtures/rules/assembly-a.yaml
#+END_SRC

* 四路录像 Replay

=ReplayPlanner= 使用 ffprobe 获取真实帧时间戳, 应用每个摄像头的时钟修正值,
然后根据 =sync_tolerance_ms= 生成四路同步 FrameSet.

#+BEGIN_SRC python
from video_pose.video_replay import ReplayPlanner

planner = ReplayPlanner.from_manifest_file(
    "fixtures/replay/session-manifest.yaml"
)
frame_sets = planner.plan()
#+END_SRC

Manifest 中的 =clock_offset_ms= 定义为需要加到源时间戳上的校正量.

* 模型适配边界

核心模型接口:

#+BEGIN_SRC python
class CameraPerceptionModel:
    def infer(self, frame, *, session_id):
        ...
#+END_SRC

模型输出统一为 =Observation=. =MultiViewPerceptionAdapter= 负责四路调用和
契约校验.

离线 MP4 已提供可选的 =OpenCVFileFrameLoader=. 该模块使用惰性 import,
因此核心规则和 Replay 测试无需安装 OpenCV. 实时 RTSP/GStreamer Loader
将在 Video Gateway 阶段实现.

* Observation Replay

#+BEGIN_SRC python
from video_pose.observation_replay import run_observation_replay

result = run_observation_replay(
    "fixtures/observations/pick-place-compliant.jsonl",
    "fixtures/rules/pick-place.yaml",
)
print(result.model_dump_json(indent=2))
#+END_SRC

* 文档

详细 C4 与技术设计见 =docs/=.
